<?php

use App\Game_buy_by_user;
use Illuminate\Database\Seeder;

class GameBuyByUserSeeder extends Seeder {
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run() {
        // factory(Game_buy_by_user::class, 1)->create()->each(function ($gameBuyByUser) {
        //     $gameBuyByUser->save();
        // });
    }
}
